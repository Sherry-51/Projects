#include "restaurant.h"
int main() {
    // Create restaurant system object
    RestaurantSystem restaurant;
    // Function to take orders and show working of the whole system
    OrderTaker(restaurant);
    return 0;
}
